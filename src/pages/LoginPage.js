import React from 'react';

import {LoginForm} from "../components";

function LoginPage(props) {

    return (
        <div className={'forms'}>
           <LoginForm/>
        </div>
    );
}

export {LoginPage};
